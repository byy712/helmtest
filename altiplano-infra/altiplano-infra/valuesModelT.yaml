# valuesModelT.yaml is used for small scale networks,
#  -------------------
# | Deployment scale |
#  ------------------
# | 5K/3k/2k/1k subscribers |
#  -------------------------
tags:
  rdof: false

global:
  clustered_opentsdb: false

altiplano-indexsearch:
  bssc-indexsearch:
    manager:
      resources:
        limits:
          cpu: 250m
          memory: 1536Mi
        requests:
          cpu: 10m
          memory: 768Mi
      java_opts: "-Xms256m -Xmx256m -Dlog4j2.formatMsgNoLookups=true"

    client:
      resources:
        limits:
          cpu: 500m
          memory: 2Gi
        requests:
          cpu: 100m
          memory: 768Mi
      java_opts: "-Xms768m -Xmx768m -Dlog4j2.formatMsgNoLookups=true"

    data:
      resources:
        limits:
          cpu: 1500m
          memory: 4Gi
        requests:
          cpu: 250m
          memory: 1536Mi
      java_opts: "-Xms1500m -Xmx1500m -Dlog4j2.formatMsgNoLookups=true"

    cbur:
      cbura:
        resources:
          limits:
            cpu: 250m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 256Mi

    jobs:
      secAdminUpgradeJob:
        resources:
          requests:
            cpu: 200m
            memory: 500Mi
          limits:
            cpu: 400m
            memory: 1Gi

  bssc-dashboards:
    dashboards:
      resources:
        limits:
          cpu: 500m
          memory: 2Gi
        requests:
          cpu: 100m
          memory: 1Gi

  bssc-indexmgr:
    curator:
      resources:
        limits:
          cpu: 200m
          memory: "1Gi"
        requests:
          cpu: "100m"
          memory: "100Mi"

  bssc-fluentd:
    fluentd:
      resources:
        requests:
          cpu: 100m
          memory: 1Gi
        limits:
          cpu: 500m
          memory: 2Gi

altiplano-mariadb:
  cbur:
    resources:
      requests:
        memory: 256Mi
        cpu: 10m
      limits:
        memory: 512Mi
        cpu: 250m
  mariadb:
    resources:
      requests:
        memory: 1536Mi
        cpu: 250m
      limits:
        memory: 3Gi
        cpu: 1
    metrics:
      resources:
        requests:
          memory: 100Mi
          cpu: 10m
        limits:
          memory: 256Mi
          cpu: 150m

    ## A customized mysqld.conf to import
    mysqld_site_conf: |-
      [mysqld]
      version = 24.6.1-REL_256-altiplano-infra-MariaDB
      skip-host-cache
      skip-name-resolve
      datadir		= /mariadb/data
      lc_messages_dir	= /usr/share/mysql
      lc_messages	= en_US
      skip-external-locking
      #
      # * Fine Tuning
      #
      max_connections		= 200
      connect_timeout		= 5
      wait_timeout		= 600
      max_allowed_packet	= 100M
      thread_cache_size       = 128
      sort_buffer_size	= 4M
      bulk_insert_buffer_size	= 16M
      tmp_table_size		= 32M
      max_heap_table_size	= 32M
      #
      # * MyISAM
      #
      # This replaces the startup script and checks MyISAM tables if needed
      # the first time they are touched. On error, make copy and try a repair.
      myisam_recover_options = BACKUP
      key_buffer_size		= 128M
      #open-files-limit	= 2000
      table_open_cache	= 400
      myisam_sort_buffer_size	= 512M
      concurrent_insert	= 2
      read_buffer_size	= 2M
      read_rnd_buffer_size	= 1M
      #
      # * Query Cache Configuration
      #
      # Cache only tiny result sets, so we can fit more in the query cache.
      query_cache_limit		= 128K
      query_cache_size		= 64M
      # for more write intensive setups, set to DEMAND or OFF
      #query_cache_type		= DEMAND
      #
      # * Logging and Replication
      #
      # Both location gets rotated by the cronjob.
      # Be aware that this log type is a performance killer.
      # As of 5.1 you can enable the log at runtime!
      #general_log_file        = /var/log/mysql/mysql.log
      #general_log             = 1
      #
      # Error logging goes to syslog due to /etc/mysql/conf.d/mysqld_safe_syslog.cnf.
      #
      # we do want to know about network errors and such
      #log_warnings		= 2
      #
      # Enable the slow query log to see queries with especially long duration
      #slow_query_log[={0|1}]
      slow_query_log_file	= /var/log/mysql/mariadb-slow.log
      long_query_time = 10
      #log_slow_rate_limit	= 1000
      #log_slow_verbosity	= query_plan

      #log-queries-not-using-indexes
      #log_slow_admin_statements
      #
      # The following can be used as easy to replay backup logs or for replication.
      # note: if you are setting up a replication slave, see README.Debian about
      #       other settings you may need to change.
      #server-id		= 1
      #report_host		= master1
      #auto_increment_increment = 2
      #auto_increment_offset	= 1
      #log_bin			= /var/log/mysql/mariadb-bin
      #log_bin_index		= /var/log/mysql/mariadb-bin.index
      # not fab for performance, but safer
      #sync_binlog		= 1
      expire_logs_days	= 1
      max_binlog_size         = 100M
      # slaves
      #relay_log		= /var/log/mysql/relay-bin
      #relay_log_index	= /var/log/mysql/relay-bin.index
      #relay_log_info_file	= /var/log/mysql/relay-bin.info
      #log_slave_updates
      #read_only
      #
      # If applications support it, this stricter sql_mode prevents some
      # mistakes like inserting invalid dates etc.
      #sql_mode		= NO_ENGINE_SUBSTITUTION,TRADITIONAL
      #
      # * InnoDB
      #
      # InnoDB is enabled by default with a 10MB datafile in /var/lib/mysql/.
      # Read the manual for more InnoDB related options. There are many!
      default_storage_engine	= InnoDB
      # you can't just change log file size, requires special procedure
      #innodb_log_file_size	= 50M
      innodb_buffer_pool_size	= 1G
      innodb_log_buffer_size	= 8M
      innodb_file_per_table	= 1
      innodb_open_files	= 400
      innodb_io_capacity	= 400
      innodb_flush_method	= O_DIRECT
      innodb_read_only_compressed = OFF
      tls_version = TLSv1.2,TLSv1.3

  admin:
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 256Mi

  maxscale:
    resources:
      requests:
        memory: 256Mi
        cpu: 100m
      limits:
        memory: 512Mi
        cpu: 500m
    metrics:
      resources:
        limits:
          cpu: 150m
          memory: 256Mi
        requests:
          cpu: 10m
          memory: 100Mi

altiplano-sso:
  resources:
    requests:
      cpu: 200m
      memory: 1Gi
    limits:
      cpu: 500m
      memory: 2Gi

altiplano-kafka:
  LogRetentionHours: "24"
  MessageMaxBytes: "314572800"
  resources:
    requests:
      cpu: 100m
      memory: 768Mi
    limits:
      cpu: 500m
      memory: 1280Mi
  JmxExporter:
    jmxResources:
      resources:
        requests:
          cpu: 10m
          memory: 50Mi
        limits:
          cpu: 100m
          memory: 256Mi

  ckaf-zookeeper:
    servers: 1
    resources:
      requests:
        cpu: 10m
        memory: 256Mi
      limits:
        cpu: 400m
        memory: 1Gi

    JmxExporter:
      jmxResources:
        resources:
          requests:
            cpu: 10m
            memory: 50Mi
          limits:
            cpu: 100m
            memory: 500Mi

altiplano-grafana:
  resources:
    requests:
      cpu: 50m
      memory: 50Mi
    limits:
      cpu: 500m
      memory: 800Mi
  grafanaUtil:
    resources:
      limits:
        cpu: 50m
        memory: 150Mi
      requests:
        cpu: 10m
        memory: 32Mi
  sidecar:
    resources:
      limits:
        cpu: 200m
        memory: 100Mi
      requests:
        cpu: 10m
        memory: 50Mi

altiplano-webdav:
  resources:
    requests:
      cpu: 10m
      memory: 256Mi
    limits:
      cpu: 2
      memory: 4Gi
  cbur:
    cbura:
      resources:
        limits:
          cpu: 250m
          memory: 512Mi
        requests:
          cpu: 50m
          memory: 10Mi

altiplano-pts:
  pushgateway:
    resources:
      limits:
        cpu: 400m
        memory: 512Mi
      requests:
        cpu: 150m
        memory: 128Mi

  kubeStateMetrics:
    resources:
      limits:
        cpu: 25m
        memory: 100Mi
      requests:
        cpu: 10m
        memory: 50Mi

  nodeExporter:
    resources:
      limits:
        cpu: 250m
        memory: 100Mi
      requests:
        cpu: 10m
        memory: 50Mi

  server:
    strategy:
      rollingUpdate:
        maxSurge: 0
        maxUnavailable: 1
    type: RollingUpdate
    resources:
      limits:
        cpu: 500m
        memory: 3Gi
      requests:
        cpu: 50m
        memory: 768Mi

  configmapReload:
    resources:
      limits:
        cpu: 150m
        memory: 32Mi
      requests:
        cpu: 10m
        memory: 32Mi

  cproUtil:
    resources:
      limits:
        cpu: 90m
        memory: 150Mi
      requests:
        cpu: 10m
        memory: 32Mi

altiplano-redis:
  server:
    resources:
      requests:
        memory: 256Mi
        cpu: 100m
      limits:
        memory: 1Gi
        cpu: 800m
    metrics:
      resources:
        requests:
          memory: 64Mi
          cpu: 10m
        limits:
          memory: 64Mi
          cpu: 50m

  admin:
    resources:
      requests:
        memory: 50Mi
        cpu: 50m
      limits:
        memory: 200Mi
        cpu: 300m

  sentinel:
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 100m
        memory: 256Mi

altiplano-opentsdb-cluster:
  altiplano-opentsdb:
    env:
      open:
        JVMARGS: "-XX:+UseG1GC -Xms1024m -Xmx1024m --add-opens
          java.base/java.nio=ALL-UNNAMED --add-opens
          java.security.jgss/sun.security.krb5=ALL-UNNAMED"
        HBASE_OPTS_GC_SETTING: "-XX:+UseG1GC -Xms1024m -Xmx1024m"
    resources:
      requests:
        cpu: 300m
        memory: 1Gi
      limits:
        cpu: 1
        memory: 3Gi
    cbur:
      cbura:
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 250m
            memory: 256Mi

  altiplano-hbase:
    hbase:
      master:
        resources:
          limits:
            cpu: 250m
            memory: 1Gi
          requests:
            cpu: 10m
            memory: 256Mi
      regionServer:
        env:
          HBASE_HEAPSIZE: 1G
          HBASE_OPTS_GC_SETTING: "-XX:+UseG1GC"
        resources:
          limits:
            cpu: 1
            memory: 3Gi
          requests:
            cpu: 250m
            memory: 1Gi

  altiplano-hdfs:
    journalNode:
      resources:
        limits:
          cpu: 200m
          memory: 1Gi
        requests:
          cpu: 10m
          memory: 256Mi
    nameNode:
      resources:
        limits:
          cpu: 200m
          memory: 1Gi
        requests:
          cpu: 10m
          memory: 256Mi
    dataNode:
      resources:
        limits:
          cpu: 800m
          memory: 1Gi
        requests:
          cpu: 50m
          memory: 256Mi

altiplano-ingress:
  metrics: true
  controller:
    hostNetwork: true
    resources:
      requests:
        memory: 123Mi
        cpu: 50m
      limits:
        memory: 512Mi
        cpu: 250m
  default404:
    resources:
      limits:
        cpu: 50m
        memory: 30Mi
      requests:
        cpu: 10m
        memory: 10Mi

altiplano-opentsdb-kcproxy:
  resources:
    requests:
      cpu: 10m
      memory: 256Mi
    limits:
      cpu: 20m
      memory: 512Mi

altiplano-cbur:
  resources:
    limits:
      cpu: 500m
      memory: 1536Mi
    requests:
      cpu: 100m
      memory: 512Mi

  k8swatcher:
    resources:
      limits:
        cpu: 300m
        memory: 400Mi
      requests:
        cpu: 150m
        memory: 200Mi

  celery:
    resources:
      limits:
        cpu: 500m
        memory: 1Gi
      requests:
        cpu: 100m
        memory: 512Mi

altiplano-oauth2-proxy:
  resources:
    limits:
      cpu: 50m
      memory: 150Mi
    requests:
      cpu: 10m
      memory: 50Mi

altiplano-installation-info:
  installationStatus:
    valuesModelT: installed
