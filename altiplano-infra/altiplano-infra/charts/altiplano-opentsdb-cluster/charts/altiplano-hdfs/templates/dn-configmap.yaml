apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "hdfs.name" . }}-datanode-fluentd-config
  
data:
  fluentd.conf: |-
{{- if .Values.dataNode.fluentd_sidecar.fluent_conf }}
{{ tpl .Values.dataNode.fluentd_sidecar.fluent_conf . | indent 4 }}
{{- end }}
