apiVersion: v1
kind: Service
metadata:
  name: {{ include "altiplano-oauth2-proxy.name" . }}
  labels:
    app: {{ include "altiplano-oauth2-proxy.name" . }}
    chart: {{ include "altiplano-oauth2-proxy.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.ports.oauthproxyPort }}
      targetPort: oauthproxyport
      protocol: TCP
      name: oauthproxyport
      {{- if and (eq "NodePort" .Values.service.type) .Values.service.ports.oauthproxyNodePort }}
      nodePort: {{ .Values.service.oauthproxyNodePort }}
      {{- end }}
  selector:
    app: {{ template "altiplano-oauth2-proxy.name" . }}
    release: {{ .Release.Name }}
#dual stack config
{{- if or ( .Values.ipFamilyPolicy ) ( .Values.ipFamilies ) }}
  {{- if .Values.ipFamilyPolicy }}
  ipFamilyPolicy: {{ .Values.ipFamilyPolicy }}
  {{- end }}
  {{- if .Values.ipFamilies }}
  ipFamilies: {{ .Values.ipFamilies | toYaml | nindent 2 }}
  {{- end }}
{{- else if or ( .Values.global.ipFamilyPolicy ) ( .Values.global.ipFamilies ) }}
  {{- if .Values.global.ipFamilyPolicy }}
  ipFamilyPolicy: {{ .Values.global.ipFamilyPolicy }}
  {{- end }}
  {{- if .Values.global.ipFamilies }}
  ipFamilies: {{ .Values.global.ipFamilies | toYaml | nindent 2 }}
  {{- end }}
{{- end }}
