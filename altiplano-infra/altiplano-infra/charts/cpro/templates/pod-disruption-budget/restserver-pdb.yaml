{{- if and .Values.restserver.enabled .Values.restserver.pdb.enabled -}}
apiVersion: {{ template "cpro.apiVersion.pdb" . }}
kind: PodDisruptionBudget
metadata:
  labels:
    {{- include "cpro.app.labels.v3" ( dict "root" . "context" .Values.restserver ) | nindent 4 }}
{{- include "cpro.labelsOrAnnotations" (tuple .Values.global.labels) | indent 4}}
  annotations:
{{- include "cpro.labelsOrAnnotations" (tuple .Values.global.annotations) | indent 4}}
  name: {{ template "cpro.prometheus.restserver.fullname" . }}
spec:
  {{- include "cpro.pdb-values" .Values.restserver.pdb | indent 2}}
  selector:
    matchLabels:
      app: {{ template "cpro.prometheus.name" . }}
      component: "{{ .Values.restserver.name }}"
      release: {{ .Release.Name }}
{{- end }}
