apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "cmdb.fullname" . }}-mariadb-fluentd-config
  labels:
    {{- include "csf-common-lib.v1.commonLabels" (tuple . "configmap") | nindent 4 }}
data:
  fluentd.conf: |-
{{- if .Values.mariadb.fluentd_sidecar.fluent_conf }}
{{ tpl .Values.mariadb.fluentd_sidecar.fluent_conf . | indent 4 }}
{{- end }}

---

