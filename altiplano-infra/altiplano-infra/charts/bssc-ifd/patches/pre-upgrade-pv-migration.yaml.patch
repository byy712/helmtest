diff --git a/templates/pre-upgrade-pv-migration-org.yaml b/templates/pre-upgrade-pv-migration.yaml
index e69ec41..379e096 100644
--- a/templates/pre-upgrade-pv-migration-org.yaml
+++ b/templates/pre-upgrade-pv-migration.yaml
@@ -30,6 +30,7 @@ metadata:
     seccomp.security.alpha.kubernetes.io/defaultProfileName: {{ .Values.global.seccompDefaultProfileName }}
 automountServiceAccountToken: false
 ---
+{{- if .Values.global.allowClusterLevelPrivileges }}
 apiVersion: {{ template "bssc-indexsearch.apiVersionRbacAuthorizatioK8sIoV1Beta1orV1" . }}
 kind: ClusterRoleBinding
 metadata:
@@ -54,6 +55,33 @@ roleRef:
   name: {{ template "bssc-indexsearch.fullname" . }}-preupgpv-cr
   apiGroup: rbac.authorization.k8s.io
 ---
+apiVersion: {{ template "bssc-indexsearch.apiVersionRbacAuthorizatioK8sIoV1Beta1orV1" . }}
+kind: ClusterRole
+metadata:
+  name: {{ template "bssc-indexsearch.fullname" . }}-preupgpv-cr
+  labels:
+{{- include "bssc-indexsearch.commonLabels" (tuple .) | nindent 4 }}
+{{- include "bssc-indexsearch.csf-toolkit-helm.labels" (tuple .) | nindent 4 }}
+  annotations:
+{{- include "bssc-indexsearch.csf-toolkit-helm.annotations" (tuple .) | nindent 4 }}
+    "helm.sh/hook": pre-upgrade
+    "helm.sh/hook-weight": "-7"
+    "helm.sh/hook-delete-policy": before-hook-creation, hook-succeeded
+    seccomp.security.alpha.kubernetes.io/allowedProfileNames: {{ .Values.global.seccompAllowedProfileNames }}
+    seccomp.security.alpha.kubernetes.io/defaultProfileName: {{ .Values.global.seccompDefaultProfileName }}
+rules:
+- apiGroups:
+  - ""
+  resources:
+  - persistentvolumes
+  verbs:
+  - create
+  - patch
+  - update
+  - get
+  - list
+{{- end }}
+---
 kind: RoleBinding
 apiVersion: {{ template "bssc-indexsearch.apiVersionRbacAuthorizatioK8sIoV1Beta1orV1" . }}
 metadata:
@@ -114,32 +142,6 @@ rules:
   - update
   - get
   - list
----
-apiVersion: {{ template "bssc-indexsearch.apiVersionRbacAuthorizatioK8sIoV1Beta1orV1" . }}
-kind: ClusterRole
-metadata:
-  name: {{ template "bssc-indexsearch.fullname" . }}-preupgpv-cr
-  labels:
-{{- include "bssc-indexsearch.commonLabels" (tuple .) | nindent 4 }}
-{{- include "bssc-indexsearch.csf-toolkit-helm.labels" (tuple .) | nindent 4 }}
-  annotations:
-{{- include "bssc-indexsearch.csf-toolkit-helm.annotations" (tuple .) | nindent 4 }}
-    "helm.sh/hook": pre-upgrade
-    "helm.sh/hook-weight": "-7"
-    "helm.sh/hook-delete-policy": before-hook-creation, hook-succeeded
-    seccomp.security.alpha.kubernetes.io/allowedProfileNames: {{ .Values.global.seccompAllowedProfileNames }}
-    seccomp.security.alpha.kubernetes.io/defaultProfileName: {{ .Values.global.seccompDefaultProfileName }}
-rules:
-- apiGroups:
-  - ""
-  resources:
-  - persistentvolumes
-  verbs:
-  - create
-  - patch
-  - update
-  - get
-  - list
 {{- end }}
 ---
 apiVersion: batch/v1
@@ -166,7 +168,11 @@ spec:
         sidecar.istio.io/inject: "false"
 {{- include "bssc-indexsearch.csf-toolkit-helm.annotations" (tuple . .Values.custom.hookJobs.pod.annotations) | indent 8 }}
     spec:
+      {{- if .Values.global.allowClusterLevelPrivileges }}
       serviceAccountName: {{ template "bssc-indexsearch.preUpgradePvMigrateServiceAccount.name" . }}
+      {{- else }}
+      serviceAccountName: {{ .Values.upgrade.preUpgradePVMigrationSAName }}
+      {{- end }}
       automountServiceAccountToken: true
       {{- if .Values.jobs.affinity }}
       affinity:
