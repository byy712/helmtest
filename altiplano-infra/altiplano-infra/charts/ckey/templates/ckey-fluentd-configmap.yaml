apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "ckey.fullName" . }}-keycloak-fluentd-config
  
data:
  fluentd.conf: |-
{{- if .Values.fluentd_sidecar.fluent_conf }}
{{ tpl .Values.fluentd_sidecar.fluent_conf . | indent 4 }}
{{- end }} 

