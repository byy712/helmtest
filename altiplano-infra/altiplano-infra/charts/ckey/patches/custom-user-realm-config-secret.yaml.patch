--- ckey-12.2.0/templates/custom-user-realm-config-secret.yaml	2024-03-27 19:37:36.794732900 +0530
+++ ckey-12.2.0-nokia-patch-1/ckey/templates/custom-user-realm-config-secret.yaml	2024-03-23 10:59:38.000000000 +0530
@@ -0,0 +1,23 @@
+apiVersion: v1
+kind: Secret
+metadata:
+  annotations:
+    "helm.sh/hook": pre-install, pre-upgrade, pre-rollback
+    "helm.sh/hook-delete-policy": before-hook-creation
+    "helm.sh/hook-weight": "-10"
+    "restartOnUpdate": "true"
+  name: {{ template "ckey.fullName" . }}-custom-user-realm-config-secret
+  labels:
+    app: {{ template "ckey.fullName" . }}
+    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
+    release: "{{ .Release.Name }}"
+    heritage: "{{ .Release.Service }}"
+    {{ include "ckey.commonLabels" . | indent 4 }}
+type: Opaque
+data:
+  dbUser: {{ .Values.dbUser | b64enc | quote }}
+{{- range $name, $value := .Values.env.secrets }}
+{{- if not (empty $value) }}
+  {{ $name }}: {{ $value | replace "password:" "" | b64enc }}
+{{- end }}
+{{- end }}
